# 本地/敏感示例数据：不纳入版本库（含子目录）
traffic/

# 部署清单：不上传到 GitHub（含子目录）
k8s/
!k8s/traffic-route-viz.yaml

# 常见系统文件
.DS_Store
Thumbs.db

# Environment / local secrets (never commit)
.env
.env.*
!.env.example

# Web runtime config (local, may contain secrets)
web/public/config.json

# Editor / IDE
.idea/
.vscode/*
!.vscode/extensions.json

# pnpm store/cache (must not commit)
.pnpm-store/

config.json

.env