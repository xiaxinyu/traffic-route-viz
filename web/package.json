{
  "name": "traffic-route-viz",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@9.15.5",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "aoai:chat": "node scripts/aoai-chat.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "spec:check": "node ../tools/check-spec.mjs",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "playwright:install": "PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT=120000 playwright install chromium",
    "e2e": "playwright test --config playwright.config.cjs",
    "e2e:ui": "playwright test --config playwright.config.cjs --ui",
    "ci": "corepack pnpm run format:check && corepack pnpm run lint && corepack pnpm run typecheck && corepack pnpm run spec:check && corepack pnpm run test && corepack pnpm run build && corepack pnpm run playwright:install && corepack pnpm run e2e"
  },
  "dependencies": {
    "html-to-image": "^1.11.13",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "reactflow": "^11.11.4",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@playwright/test": "1.53.0",
    "@types/node": "^22.15.3",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "dotenv": "^17.4.2",
    "jsdom": "^26.1.0",
    "openai": "^6.37.0",
    "typescript": "~6.0.3",
    "vite": "^6.0.3",
    "vitest": "^3.2.4"
  }
}
