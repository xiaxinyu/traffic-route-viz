---
description: Core project context + must-follow requirements
alwaysApply: true
---

# traffic-route-viz core rules

## Must read (source of truth)

- `HARNESS_ENGINEERING.md`（需求、UX、**画布 Harness**、DoD）
- `PROJECT_GUIDE.md`（workflow, repo map, common commands）

## Working agreement

- Treat `HARNESS_ENGINEERING.md` as acceptance criteria. If behavior conflicts, update code **and** update the doc in the same change.
- Prefer small, reviewable changes. After meaningful edits, ensure `web/` builds (`pnpm run build`) and no new lints.
- Keep changes aligned with current architecture:
  - Parse: `web/src/k8sParser.ts`
  - Graph/layout: `web/src/buildGraph.ts`
  - Node UI: `web/src/FlowNodes.tsx`
  - Import/UI: `web/src/App.tsx`

## Non-goals (unless explicitly requested)

- Do not add new K8s resource kinds (Gateway API, EndpointSlice) without updating requirements + DoD.
